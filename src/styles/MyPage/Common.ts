import { css } from '@emotion/react';
import { TwoButtonsContainer } from '../Common/TwoButtonsContainer';

export const BottomButtonsContainer = css(TwoButtonsContainer, {
  marginTop: '6.5rem',
});
